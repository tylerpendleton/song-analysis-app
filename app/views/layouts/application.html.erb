<!DOCTYPE html>
<html>
  <head>
    <title>SongAnalysisApp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%= link_to 'Song Analysis', root_path, class: 'navbar-brand' %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
	<span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">
	<span class="navbar-text mr-auto">
	  Find information about any song!
	</span>
	  <% if @spotify_user != nil %>
	    <span class="navbar-text">
	      Welcome <%= @spotify_user.email %>! <%= link_to 'Log Out', auth_spotify_sign_out_path %>
	    </span>
	  <% else %>
	    <%= link_to 'Sign in with Spotify!', '/auth/spotify', class: 'navbar-link' %>
	  <% end %>
	</ul>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
